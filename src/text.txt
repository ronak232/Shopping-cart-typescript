<Suspense fallback="Loading...">
                <PagesProductList />
              </Suspense>


              // Lazy Loading the store page
const PagesProductList = React.lazy(async () => ({
  default: (await import("./pages/ProductsList")).default,
}));

 //   const productSearchFilter = filterSearchResults.filter((product) => {
      //     return Object.values(product)
      //       .join(" ")
      //       .toLowerCase()
      //       .includes(product.title.toLowerCase());
      //   });
      //   setFilterSearchResults(productSearchFilter);
      // } else {
      //   setFilterSearchResults(products);